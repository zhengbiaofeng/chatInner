# 内网聊天室 3.0 (赛博/科技风) 功能演进开发手册

本项目将在 2.0 全栈版本的基础上，吸收优秀开源 IM 的特性，引入多频道、AI 助手、在线状态及消息撤回/回执等高级能力，进一步打造极致的内网协同体验。

## 阶段六：多频道与在线状态体系 (Channels & Presence)
- [ ] 6.1 **后端数据模型升级**：在 `types/index.ts` 中新增 `Channel` (频道) 模型，更新 `db.json` 结构以支持多房间及私聊数据隔离。
- [ ] 6.2 **Socket 房间逻辑重构**：在 `server.ts` 中实现用户加入/离开不同频道的逻辑，并广播频道的在线用户列表（Presence）。
- [ ] 6.3 **前端频道导航栏**：在聊天主界面左侧（或顶部）增加频道切换面板（如 `#general`, `#dev`, `#摸鱼区`），支持新建公共频道。
- [ ] 6.4 **在线人员列表展示**：在界面右侧或频道信息中展示当前频道的在线用户，头像旁亮起赛博绿色指示灯。

## 阶段七：消息高阶管控 (Recall & Read Receipt)
- [ ] 7.1 **消息撤回机制 (Backend)**：增加撤回接口与 Socket 事件，验证 2 分钟时效限制及用户权限（管理员可强撤）。
- [ ] 7.2 **消息撤回展现 (Frontend)**：在消息列表中增加“撤回”操作项，撤回后渲染为科技风系统提示 `[操作员已销毁一条加密信息]`。
- [ ] 7.3 **消息回执设计**：记录每条消息在频道内的已读人数，前端在气泡旁展示极简的已读状态小标记（如 `👁️ 2`）。

## 阶段八：内网大模型 AI 助手接入 (AI Stream Integration)
- [ ] 8.1 **AI 助手角色预置**：在系统中预置系统级账号 `@NexusBot` (AI 助手)。
- [ ] 8.2 **大模型接口打通**：在后端配置内网千问 3.5 大模型的 API 对接逻辑（支持 OpenAI 兼容格式或 HTTP直连）。
- [ ] 8.3 **流式消息传输 (Stream)**：重构 Socket 逻辑，当用户 @ 机器人时，后端请求大模型并通过 Socket 将 Token 逐字流式推送到前端。
- [ ] 8.4 **前端打字机渲染**：前端支持接收流式片段并实时渲染 Markdown，呈现黑客帝国般的“打字机”输出视觉效果。

## 阶段九：工程化测试与 3.0 交付 (Testing & V3 Release)
- [ ] 9.1 **端到端测试更新**：完善 `smoke-test.js`，加入多频道切换、撤回消息的核心逻辑校验。
- [ ] 9.2 **全面联调与 UI 走查**：确保新加的左侧栏与原有赛博 UI 风格完全融合，无白屏、流式输出不卡顿。
- [ ] 9.3 **生产构建与打包**：执行 `npm run build`，输出最终的 `intranet-chat-v3-ai.7z` 离线部署包。
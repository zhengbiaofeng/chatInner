# 默认端口建议用不常占用的（例如 32123）。
# 若端口被占用，程序会自动尝试 +1 递增寻找可用端口。
PORT=32123
# 数据与上传文件存放目录（相对或绝对都可以）
DATA_DIR=./data
# 单文件最大上传大小（MB）
MAX_UPLOAD_MB=500

# 可选：允许同事自助注册（默认关闭，推荐由管理员创建账号）
# ALLOW_SELF_REGISTER=1
# 可选：自行指定 JWT_SECRET；不填则会在 data/jwt_secret.txt 自动生成并持久化
# JWT_SECRET=change_me
